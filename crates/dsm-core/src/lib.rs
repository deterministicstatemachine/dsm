//! # dsm-core
//!
//! Core state machine logic for the DSM decentralized state machine.
//!
//! This crate defines:
//! - State types and state transition rules
//! - Validation logic for deterministic state transitions
//! - The core `StateMachine` trait

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
