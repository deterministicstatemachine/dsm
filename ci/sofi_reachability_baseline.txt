# Gate G1 baseline (ci/sofi_reachability.py): pub fns under CORE/sofi that the
# rebuild has not wired into production yet. One line per function, with the
# rebuild step expected to wire it. A step that wires one DELETES its line;
# R14 deletes the file and G1 passes with no exceptions.
#
# TWO ENTRIES, BOTH THE SAME HOLE (2026-09-24).
#
# Storage spec §9 rules 9 and 11: the client that relies on a `Final` keeps
# its completion proof, and a verifier checks a presented proof online, from
# its own reads of the seats. The SDK keeps the proofs (sofi_exercise,
# sofi_registration). The checks below are those verifiers. No party receives
# a kept proof to check yet, so neither has a production caller; the facade
# that presents a completion proof wires them and deletes these lines.
#
# It grew at R14 before it shrank, and both are the point. The matcher fix
# (spec §41.3 correction 2, owed before step 5 and not made) required a call
# to be IN SCOPE, and four functions that had never been wired or baselined
# appeared: they had been passing the old bare-name matcher through unrelated
# functions called `get`, `verify`, `reachable` and `apply` elsewhere in the
# workspace. Three to seven. Then seven to zero, and not one of them by being
# wired:
#
#   * the trader's persistent node store — `apply`, `commit_shadow`, `get`,
#     `prove`, `verify`, `reachable` — is DELETED. Spec §44.4 ruled that the
#     responsibility is replaced, not the dead implementation resurrected: a
#     node store returns leaf VALUES, and the vault head past its genesis
#     needs the leaf PREIMAGES a later acquisition consumes, so wiring them
#     would not have made one past-genesis trade work.
#   * `genesis_accepted` is DELETED from Rust and the rule it stated now lives
#     in the formal layer alone (§44.4 again, and `GenesisAccepted` in Lean).
#
# R14 deletes this file when the rest of its deliverable is real: the
# post-genesis path that produces `ParentStatus::Orphaned`, the production
# SoFi facade that constructs or loads a `TraderCore` and a `DlvCore`, and the
# twelve device scenarios. Until then an empty list is an honest statement,
# where a deleted file would be a claim.
dsm_client/deterministic_state_machine/dsm/src/sofi/exercise.rs check_attempt_completion
dsm_client/deterministic_state_machine/dsm/src/sofi/registration.rs check_fulfillment_completion
#
# A THIRD ENTRY (2026-09-25), found when the gate started reading whole files
# (ci/production_text.py) instead of stopping at a file's first `#[cfg(test)]`
# item: `verify_signed_object` is the one verifier that binds a signed SoFi
# object's key to an EXPECTED signer, and no production path calls it. P is
# verified under the key it carries, a setup under P's key, and F under P's
# key (MR-SOFI-0151; the shortcut audit of 2026-09-25, "No production SoFi
# path binds a precommit's key to the trader it names"). The change that
# binds the claimant key to the trader's proven AK wires it and deletes this
# line.
dsm_client/deterministic_state_machine/dsm/src/sofi/signature.rs verify_signed_object
