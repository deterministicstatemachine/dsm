\* Malformed key-split family at a SINGLE honest verifier. The static Safety
\* property is violated (see DSM_GuardedMC_Fork.cfg); this config checks only
\* the realized-history invariant. Once the first receipt consumes k1,
\* LinearityOK disables the conflicting candidate, so the local ledger never
\* forks (paper Prop 11). Expected: No error.
SPECIFICATION Spec

INVARIANT TypeOK
INVARIANT RealizedHistoryUnique
